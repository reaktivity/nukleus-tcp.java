language: java
jdk:
  - oraclejdk8
addons:
  apt:
    packages:
      # Getting core dump with java 1.8.0_31 and ByteMan
      - oracle-java8-installer

install: true
script: mvn -B verify
cache:
  directories:
    - $HOME/.m2
branches:
  except:
  - /^release\/.*$/
